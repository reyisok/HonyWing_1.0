<Window x:Class="HonyWing.UI.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="400"
        Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource DarkBlueBackgroundBrush}"
        Foreground="{StaticResource WhiteForegroundBrush}">
    
    <Grid Margin="20" Background="{StaticResource DarkBlueBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 提示消息 -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Message}"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="{StaticResource WhiteForegroundBrush}"/>
        
        <!-- 输入框 -->
        <TextBox Grid.Row="2"
                 x:Name="InputTextBox"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource DarkBlueTextBoxStyle}"
                 FontSize="14"
                 Padding="8"
                 Height="32"
                 VerticalContentAlignment="Center"/>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            
            <Button Content="确定"
                    Width="80"
                    Height="32"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OkButton_Click"
                    Style="{StaticResource DarkBlueButtonStyle}"/>
            
            <Button Content="取消"
                    Width="80"
                    Height="32"
                    IsCancel="True"
                    Click="CancelButton_Click"
                    Style="{StaticResource DarkBlueButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>